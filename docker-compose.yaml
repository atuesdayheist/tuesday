services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app/api
    environment:
      - PYTHONPATH=/app/api
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload
