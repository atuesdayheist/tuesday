services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src # hot-reload in dev
    environment:
      - PYTHONPATH=/app/src
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
